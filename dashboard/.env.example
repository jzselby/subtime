# Required — this site has nothing to fall back to without these; it's a
# thin, always-online reader with no local data of its own. Same Supabase
# project as app/.env.example's VITE_SUPABASE_URL/VITE_SUPABASE_ANON_KEY.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
